---
layout: layouts/page.njk
eyebrow: Hobbies
title: Hobbies
description: Factorio, crosswords, minesweeper, and other problem-solving games.
---
{% from "partials/writings-list.njk" import writingsList %}

<section id="hobbies-list" data-nav-section="/hobbies/">
  <div class="container">
    <div class="reveal">
      <p class="eyebrow">Problem-solving games</p>
      <div class="cards-grid">
        {% for hobby in hobbies %}
          <article class="card">
            <div class="placeholder-block" style="aspect-ratio: 3 / 2; margin-bottom: var(--sp-2);">
              <span>{{ hobby.filename }} — {{ hobby.dimensions }}</span>
            </div>
            <h3>{{ hobby.title }}</h3>
            <p>{{ hobby.description }}</p>
          </article>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<section id="writings" data-nav-section="/hobbies/">
  <div class="container">
    <div class="reveal">
      <h2>Writings</h2>
      {{ writingsList(collections.writings | byTag("hobbies") | reverse, "Notes on whatever puzzle has my attention this week — coming soon.") }}
    </div>
  </div>
</section>
